# For details on buf.yaml configuration, visit https://buf.build/docs/configuration/v2/buf-yaml
version: v2
lint:
  use:
    - STANDARD
  except:
    - ENUM_VALUE_PREFIX
    - ENUM_ZERO_VALUE_SUFFIX
    - ENUM_NO_ALLOW_ALIAS
breaking:
  use:
    - FILE
modules:
  - path: .
    name: buf.build/tcn/exileapi
deps:
  - buf.build/googleapis/googleapis
